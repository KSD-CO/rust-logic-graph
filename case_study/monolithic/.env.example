# =============================================================================
# Multi-Server Database Configuration
# =============================================================================
# Each database can be on a different server/host with different credentials
# This allows true distributed data architecture in a monolithic application

# -----------------------------------------------------------------------------
# Default Database (Fallback)
# -----------------------------------------------------------------------------
# Used if specific database config is not provided
DB_HOST="localhost"
DB_PORT="5432"
DB_USER="postgres"
DB_PASSWORD="postgres"

# -----------------------------------------------------------------------------
# OMS Database Server
# -----------------------------------------------------------------------------
# Order Management System - Historical data and demand trends
OMS_DB_HOST="localhost"
OMS_DB_PORT="5432"
OMS_DB_USER="postgres"
OMS_DB_PASSWORD="postgres"
OMS_DB_NAME="oms_db"

# Example: AWS RDS
# OMS_DB_HOST="oms-prod.c9akciq32.us-west-2.rds.amazonaws.com"
# OMS_DB_PORT="5432"
# OMS_DB_USER="oms_admin"
# OMS_DB_PASSWORD="<your-secure-password>"
# OMS_DB_NAME="oms_production"

# -----------------------------------------------------------------------------
# Inventory Database Server
# -----------------------------------------------------------------------------
# Inventory Management - Stock levels and warehouse data
INVENTORY_DB_HOST="localhost"
INVENTORY_DB_PORT="5432"
INVENTORY_DB_USER="postgres"
INVENTORY_DB_PASSWORD="postgres"
INVENTORY_DB_NAME="inventory_db"

# Example: Google Cloud SQL
# INVENTORY_DB_HOST="34.82.145.92"
# INVENTORY_DB_PORT="5432"
# INVENTORY_DB_USER="inventory_service"
# INVENTORY_DB_PASSWORD="<your-secure-password>"
# INVENTORY_DB_NAME="inventory_prod"

# -----------------------------------------------------------------------------
# Supplier Database Server
# -----------------------------------------------------------------------------
# Supplier Management - Supplier info, pricing, and lead times
SUPPLIER_DB_HOST="localhost"
SUPPLIER_DB_PORT="5432"
SUPPLIER_DB_USER="postgres"
SUPPLIER_DB_PASSWORD="postgres"
SUPPLIER_DB_NAME="supplier_db"

# Example: Azure Database for PostgreSQL
# SUPPLIER_DB_HOST="supplier-db.postgres.database.azure.com"
# SUPPLIER_DB_PORT="5432"
# SUPPLIER_DB_USER="supplier_admin@supplier-db"
# SUPPLIER_DB_PASSWORD="<your-secure-password>"
# SUPPLIER_DB_NAME="suppliers"

# -----------------------------------------------------------------------------
# UOM Database Server
# -----------------------------------------------------------------------------
# Unit of Measurement - Conversion factors between units
UOM_DB_HOST="localhost"
UOM_DB_PORT="5432"
UOM_DB_USER="postgres"
UOM_DB_PASSWORD="postgres"
UOM_DB_NAME="uom_db"

# Example: On-premise server
# UOM_DB_HOST="10.0.1.50"
# UOM_DB_PORT="5432"
# UOM_DB_USER="uom_readonly"
# UOM_DB_PASSWORD="<your-secure-password>"
# UOM_DB_NAME="reference_data"

# =============================================================================
# Usage Examples
# =============================================================================

# --- Local Development (All on localhost) ---
# Use the default localhost settings above

# --- Multi-Container Docker ---
# OMS_DB_HOST="oms-postgres"        # Docker container name
# INVENTORY_DB_HOST="inv-postgres"
# SUPPLIER_DB_HOST="sup-postgres"
# UOM_DB_HOST="uom-postgres"

# --- Different Ports on Same Server ---
# OMS_DB_PORT="5432"
# INVENTORY_DB_PORT="5433"
# SUPPLIER_DB_PORT="5434"
# UOM_DB_PORT="5435"

# --- Multi-Cloud Deployment ---
# OMS_DB_HOST="oms-prod.rds.amazonaws.com"      # AWS
# INVENTORY_DB_HOST="34.82.145.92"              # GCP
# SUPPLIER_DB_HOST="supplier.postgres.azure.com" # Azure
# UOM_DB_HOST="uom-db.company.com"              # On-premise

# --- Read Replicas ---
# Use read replicas for heavy read operations
# OMS_DB_HOST="oms-read-replica.rds.amazonaws.com"
# INVENTORY_DB_HOST="inventory-replica.c9akciq32.us-west-2.rds.amazonaws.com"

# =============================================================================
# Security Notes
# =============================================================================
# 1. Never commit this file with real credentials to version control
# 2. Use strong passwords for production databases
# 3. Use SSL/TLS connections for remote databases (add ?sslmode=require)
# 4. Use read-only users where possible
# 5. Rotate credentials regularly
# 6. Use secrets management (AWS Secrets Manager, HashiCorp Vault, etc.)

# =============================================================================
# Connection String Format
# =============================================================================
# postgres://user:password@host:port/database
# Example: postgres://admin:secret@db.example.com:5432/mydb
#
# Optional parameters:
# - ?sslmode=require          # Require SSL/TLS
# - ?connect_timeout=10       # Connection timeout in seconds
# - ?application_name=myapp   # Application identifier
